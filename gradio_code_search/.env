# Endpoint URL of your running embedding service (cache proxy)
EMBEDDING_ENDPOINT_URL=http://localhost:8080/embed
# Embedding dimension (MUST match the model used by the endpoint)
EMBEDDING_DIMENSION=384